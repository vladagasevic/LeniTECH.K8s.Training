apiVersion: apps/v1
kind: Deployment
metadata:
  name: moj-prvi-deployment
spec:
  replicas: 3

  # strategy:
  #   type: RollingUpdate
  #   rollingUpdate:
  #     maxUnavailable: 2
  #     maxSurge: 2

  selector:
    matchLabels:
      app: training
  template:
    metadata:
      labels:
        app: training
    spec:
      containers:
      - name: treningapp
        image: vlajko83/treningapp:v1
        imagePullPolicy: Always
        # env:
        # - name: WELCOME_MSG         
        #   valueFrom:
        #     configMapKeyRef:
        #       name: moj-prvi-config
        #       key: WELCOME_MSG
        # - name: APP_COLOR          
        #   valueFrom:
        #     configMapKeyRef:
        #       name: moj-prvi-config
        #       key: APP_COLOR
        # - name: APP_VERSION       
        #   valueFrom:
        #     configMapKeyRef:
        #       name: moj-prvi-config
        #       key: APP_VERSION
        # - name: LOZINKA_BAZE
        #   valueFrom:
        #     secretKeyRef:
        #       name: moj-prvi-secret
        #       key: DB_PASSWORD
      # volumes:
      # - name: storage-podaci
      #   persistentVolumeClaim:
      #     claimName: moj-prvi-pvc
        ports:
        - containerPort: 5000